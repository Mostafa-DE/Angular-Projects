import { MyNgClassDirective } from './my-ng-class.directive';

describe('MyNgClassDirective', () => {
  it('should create an instance', () => {
    const directive = new MyNgClassDirective();
    expect(directive).toBeTruthy();
  });
});
